# 项目设计文档

## 1. 项目概述

### 1.1 项目背景
本项目是一个基于Go语言开发的WebSocket服务，用于支持实时通信功能，如聊天、实时数据推送等。项目设计目标是支持1万同时在线用户，1万QPS的请求处理能力，并具备高可用性和可扩展性。

### 1.2 技术栈
- **开发语言**：Go 1.20+
- **Web框架**：Gorilla WebSocket
- **负载均衡**：Nginx + Keepalived
- **缓存/状态存储**：Redis
- **部署环境**：云服务器（Ubuntu 20.04）

### 1.3 核心功能
- 实时WebSocket通信
- 会话状态管理
- 负载均衡
- 高可用性
- 故障自动转移

## 2. 架构设计

### 2.1 整体架构
```
客户端 → 虚拟IP（VIP） → 主Nginx → WebSocket节点1
                    → WebSocket节点2
                    → WebSocket节点3
所有节点 ↔ Redis（状态存储 + 主从复制）
```

### 2.2 核心设计原则
- **高内聚、低耦合**：模块之间通过清晰的接口通信
- **无状态设计**：节点本身不存储状态，状态存储在Redis中
- **高可用性**：关键组件（Nginx、Redis）实现主备备份
- **可扩展性**：支持动态添加或移除节点
- **简单有效**：避免过度设计，优先选择成熟稳定的技术方案

## 3. 模块划分

### 3.1 核心模块

| 模块名称 | 主要职责 | 技术栈 | 部署位置 |
|----------|----------|--------|----------|
| **WebSocket服务** | 处理WebSocket连接、消息收发 | Go + Gorilla WebSocket | 云服务器节点 |
| **负载均衡** | 请求分发、负载均衡 | Nginx + Keepalived | 云服务器节点 |
| **会话管理** | 存储会话状态、实现状态同步 | Redis | 云服务器节点 |
| **配置管理** | 集中管理配置信息 | 配置文件 + 环境变量 | 云服务器节点 |

### 3.2 模块详细设计

#### 3.2.1 WebSocket服务模块
- **核心功能**：
  - 处理WebSocket连接的建立和关闭
  - 消息的接收和发送
  - 会话状态的管理
  - 心跳机制
- **目录结构**：
  ```
  ws_server/
  ├── main.go          # 程序入口
  ├── server.go        # WebSocket服务器实现
  ├── handler.go       # 消息处理
  ├── session.go       # 会话管理
  └── config.go        # 配置管理
  ```

#### 3.2.2 负载均衡模块
- **核心功能**：
  - 请求的负载均衡分配
  - 节点健康检查
  - 故障自动转移
- **配置文件**：
  - Nginx配置：`/etc/nginx/nginx.conf`
  - Keepalived配置：`/etc/keepalived/keepalived.conf`

#### 3.2.3 会话管理模块
- **核心功能**：
  - 会话状态的存储和读取
  - 会话状态的备份和恢复
  - 会话过期管理
- **数据结构**：
  ```go
  type SessionState struct {
      UserID   string    `json:"user_id"`
      NodeID   string    `json:"node_id"`
      LastSeen time.Time `json:"last_seen"`
      // 其他会话数据
  }
  ```

## 4. 部署方案

### 4.1 服务器配置

| 服务器角色 | 数量 | 配置 | 内部IP | 公网IP |
|------------|------|------|--------|--------|
| 主Nginx + WebSocket节点 + Redis主 | 1 | 2核4G | 192.168.1.10 | 111.111.111.111 |
| 备Nginx + WebSocket节点 + Redis从 | 1 | 2核4G | 192.168.1.11 | 222.222.222.222 |

### 4.2 网络配置

- **VPC**：192.168.0.0/16
- **子网**：192.168.1.0/24
- **虚拟IP（VIP）**：192.168.1.100

### 4.3 安全组配置

| 端口 | 协议 | 源地址 | 描述 |
|------|------|--------|------|
| 22 | TCP | 0.0.0.0/0 | SSH访问 |
| 80 | TCP | 0.0.0.0/0 | HTTP访问 |
| 8080 | TCP | 0.0.0.0/0 | WebSocket服务 |
| 6379 | TCP | 192.168.0.0/16 | Redis访问 |
| 11211 | TCP | 192.168.0.0/16 | Redis访问 |

### 4.4 部署步骤

#### 4.4.1 环境准备
1. 购买云服务器
2. 配置VPC和安全组
3. 设置内部IP

#### 4.4.2 核心组件部署
1. **Nginx安装与配置**
   - 安装Nginx：`apt update && apt install -y nginx`
   - 配置WebSocket负载均衡
   - 启动Nginx服务

2. **Keepalived安装与配置**
   - 安装Keepalived：`apt install -y keepalived`
   - 配置主备节点
   - 配置健康检查脚本
   - 启动Keepalived服务

3. **Redis安装与配置**
   - 安装Redis：`apt install -y redis-server`
   - 配置主从复制
   - 启动Redis服务

4. **WebSocket服务部署**
   - 编译Go代码：`go build -o ws_server main.go`
   - 创建系统服务
   - 启动WebSocket服务

#### 4.4.3 测试与验证
1. WebSocket连接测试
2. 负载均衡测试
3. 故障转移测试
4. 会话状态测试

## 5. 实施规划

### 5.1 阶段划分

| 阶段 | 时间 | 主要任务 |
|------|------|----------|
| **需求分析** | 1周 | 需求收集、分析和确认 |
| **架构设计** | 1周 | 架构设计、模块划分、技术选型 |
| **开发实现** | 2周 | 核心功能开发、单元测试 |
| **部署测试** | 1周 | 环境部署、集成测试、性能测试 |
| **上线运行** | 1天 | 正式上线、监控配置 |
| **运维优化** | 持续 | 监控维护、性能优化 |

### 5.2 里程碑
- **M1**：完成架构设计文档
- **M2**：完成核心功能开发
- **M3**：完成测试环境部署
- **M4**：完成性能测试
- **M5**：正式上线

## 6. 待优化项

### 6.1 性能优化
- [ ] 优化WebSocket服务的并发处理能力
- [ ] 实现WebSocket连接池管理
- [ ] 优化Redis缓存策略，减少网络IO
- [ ] 实现消息批量处理，提高吞吐量

### 6.2 可用性优化
- [ ] 实现Redis的哨兵模式或集群模式
- [ ] 增加Nginx的健康检查频率
- [ ] 实现WebSocket服务的优雅关闭
- [ ] 配置自动扩容机制

### 6.3 安全性优化
- [ ] 实现WebSocket连接的认证和授权
- [ ] 配置HTTPS/WSS加密通信
- [ ] 实现IP黑名单和限流机制
- [ ] 加固服务器安全配置

### 6.4 监控与运维
- [ ] 实现Prometheus + Grafana监控
- [ ] 配置日志收集和分析系统
- [ ] 实现自动化部署脚本
- [ ] 编写详细的运维手册

### 6.5 功能扩展
- [ ] 实现消息持久化
- [ ] 支持消息广播功能
- [ ] 实现多房间/频道功能
- [ ] 支持消息历史记录查询

## 7. 风险评估

### 7.1 技术风险
- **WebSocket连接数限制**：单个节点的连接数受限于操作系统的文件描述符限制，需要调整系统参数
- **Redis性能瓶颈**：Redis的读写性能可能成为瓶颈，需要优化缓存策略
- **网络延迟**：跨节点通信可能导致网络延迟，需要优化架构设计

### 7.2 部署风险
- **云服务器故障**：云服务器可能出现硬件故障，需要实现高可用机制
- **配置错误**：配置错误可能导致服务不可用，需要严格的配置管理和测试
- **安全漏洞**：系统可能存在安全漏洞，需要定期进行安全审计和更新

### 7.3 运维风险
- **监控盲区**：可能存在监控盲区，导致故障无法及时发现
- **日志不完整**：日志不完整可能导致故障定位困难
- **缺乏应急预案**：缺乏应急预案可能导致故障恢复时间过长

## 8. 总结

本项目设计文档详细描述了WebSocket服务的架构设计、模块划分、部署方案和实施规划。项目采用了成熟稳定的技术栈，实现了高可用性和可扩展性的设计目标。通过合理的模块划分和部署方案，确保了系统能够支持1万同时在线用户和1万QPS的请求处理能力。

项目设计遵循了简单有效的原则，避免了过度设计，同时预留了扩展空间，便于后续功能扩展和性能优化。通过实施规划的阶段划分和里程碑设置，确保了项目能够按时按质完成。

在项目实施过程中，需要密切关注风险评估中提到的各项风险，并采取相应的措施进行防范和应对。同时，根据实际运行情况，不断优化系统性能和可用性，确保系统能够稳定可靠地运行。
